// Navbar.jsx
import React from 'react';
import Button from './Button';

const Navbar = () => {
    return (
        <div>
            <button>Navbar</button>
            <Button />
        </div>
    );
}

export default Navbar;
